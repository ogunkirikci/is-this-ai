[project]
name = "isthisai-bot"
version = "0.1.0"
description = "X (Twitter) bot that analyzes media for AI-generated content"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn[standard]>=0.34.0",
    "pydantic>=2.10.5",
    "pydantic-settings>=2.7.1",
    "redis>=5.2.1",
    "rq>=2.1.0",
    "requests>=2.32.3",
    "python-multipart>=0.0.20",
    "Pillow>=10.4.0",
    "numpy>=2.1.3",
    "torch>=2.0",
    "transformers>=4.40",
    "huggingface-hub>=0.20",
]

[project.scripts]
# Optional: CLI entry points
